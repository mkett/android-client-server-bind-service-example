<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- create a custom permission -->
    <permission
        android:description="@string/permission_description"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/permission_label"
        android:name="com.example.permission.RANDOM_NUMBER_PERMISSION"
        android:protectionLevel="dangerous"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ServerProvidesService"
        tools:targetApi="31">

        <!-- add the necessary custom RANDOM_NUMBER_PERMISSION permission to the service -->
        <service
            android:permission="com.example.permission.RANDOM_NUMBER_PERMISSION"
            android:name=".RandomNumberService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="RandomNumberService"/>
            </intent-filter>
        </service>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>